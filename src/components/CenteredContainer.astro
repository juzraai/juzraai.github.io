---
import type { HTMLTag } from 'astro/types';

type Props = {
	as?: HTMLTag;
	class: string;
};
const Tag = Astro.props.as ?? 'div';
---

<Tag class:list={['container mx-auto max-w-[84rem] px-4 lg:px-8', Astro.props.class]}>
	<slot />
</Tag>
