---
import { type AltLinks, blogTitle, portfolioTitle, type Lang } from '../content/config';
import CenteredContainer from './CenteredContainer.astro';
import DarkModeToggle from './DarkModeToggle.astro';
import LangSwitcher from './LangSwitcher.astro';
import NavLink from './NavLink.astro';

type Props = {
	alt: AltLinks;
	lang: Lang;
};
const { alt, lang } = Astro.props;
---

<CenteredContainer
	as="nav"
	class="flex items-center justify-between py-3 text-2xl tracking-tight"
>
	<div class="flex gap-8">
		<div class="font-bold">
			<a href={lang === 'hu' ? '/' : `/${lang}/`}>Zsolt</a>
		</div>
		<div class="flex gap-4">
			<NavLink
				label={blogTitle[lang]}
				lang={lang}
				path="blog"
			/>
			<NavLink
				label={portfolioTitle[lang]}
				lang={lang}
				path="projects"
			/>
		</div>
	</div>
	<div class="flex items-center gap-4">
		<LangSwitcher
			alt={alt}
			current={lang}
		/>
		<DarkModeToggle />
	</div>
</CenteredContainer>
